import { Chicle } from "@next/font/google";
import { Space_Mono } from "@next/font/google";

export const chicle = Chicle({
    weight: ['400'],
    style: ['normal'],
    subsets: ['latin'],
    display: 'swap',
  });

export const s_mono = Space_Mono({
    weight: ['400','700'],
    style: ['normal'],
    subsets: ['latin'],
    display: 'swap',
  });